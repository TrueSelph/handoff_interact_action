# handoff_interact_action
This action is designed to seamlessly handle queries the agent cannot resolve independently. It silently transfers unresolved queries to a human operator. Once the required information or resolution is obtained, the action updates the relevant store with the response and notifies the user.
